#!/usr/bin/env python3

import numpy

class Drone:

    # x and y
    dim = 2
    pos = np.zeros(dim)
    vel = np.zeros(dim)
    acc = np.zeros(dim)
    wind_vel = np.zeros(dim)
    
    def __init__():
